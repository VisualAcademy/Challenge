//[?] 논리 자료형: 참(True) 또는 거짓(False) 값 저장
#include <stdio.h>
#include <stdbool.h> // bool, true(1), false(0)

int main(void)
{
    bool bln = true; // 참(1)
    printf("%d\n", bln); // 1

    bool isFalse = false; // 거짓(0)
    printf("%d\n", isFalse); // 0

    return 0;
}
